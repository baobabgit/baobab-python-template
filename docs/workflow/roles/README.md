# Rôles IA — emplacement déplacé

Les fiches de rôle ont été consolidées dans :

**`docs/ai_workflow/roles/`** ← emplacement canonique

Ce dossier n'est conservé que pour ne pas casser d'éventuels liens existants.
Référez-vous uniquement aux fichiers sous `docs/ai_workflow/roles/`.
